# Blog (future)

This directory is a placeholder for a future blog. Drop posts here as MDX files when ready.

Suggested structure:
- `app/blog/page.tsx` — index listing.
- `app/blog/[slug]/page.tsx` — MDX-driven post template.
- `content/blog/<slug>.mdx` — actual posts.

Not built in v1 to keep scope tight; sitemap.ts will need an update when added.
